Algoritmo "tabuada"

Var

    i, n, b : inteiro

Inicio

     escreva("Deseja a tabuada para qual valor? ")
     leia(n)
     
     para i de 1 ate 10 faca
         b <- n * i
         escreval(n, " x", i, " =", b)
     fimpara
     
    
Fimalgoritmo